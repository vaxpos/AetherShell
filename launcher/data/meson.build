# Install CSS
install_data(
  'style/launcher.css',
  install_dir : join_paths(
    get_option('datadir'), meson.project_name(), 'style'
  )
)

# Install .desktop
install_data(
  'launcher.desktop',
  install_dir : join_paths(get_option('datadir'), 'applications')
)
